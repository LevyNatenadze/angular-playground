import { Routes } from "@angular/router";
import { SettingsComponent } from "./feature/settings.component";

export const settingsRoutes: Routes = [
    {
        path: '',
        component: SettingsComponent    
    }
]